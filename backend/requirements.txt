# GeoRecon AI backend — full stack from CLAUDE.md (unpinned; time-boxed prototype)

# Web API
fastapi
uvicorn[standard]
python-multipart        # multipart/form-data uploads
psutil                  # kill the runner + COLMAP child process tree on cancel
pydantic
pydantic-settings

# --- SfM / MVS backend: choose ONE per platform (see CLAUDE.md) ---
pycolmap            # CPU wheel; fine for keyframe-count SfM on Windows/macOS
# pycolmap-cuda12   # Linux / WSL2 / Colab, CUDA 12 — GPU SIFT + dense
# Windows GPU dense: install COLMAP 4.x CUDA CLI separately (not pip-installable),
#          then set GEORECON_COLMAP_BACKEND=cli

# CV / 3D
opencv-python-headless
pillow
numpy
pandas
scipy
trimesh                 # mesh cleanup ops (no open3d — no cp313 wheel)
fast-simplification     # quadric decimation

# Geo
pyproj          # WGS84 <-> ECEF/ENU, UTM
laspy
rasterio

# AI — dynamic-object segmentation masks
ultralytics

# Dev / test
pytest
